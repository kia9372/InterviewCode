import { Subjects } from "../subscribs";
export interface OrderCancelledEvent {
    subject: Subjects.OrderCanceled;
    data: {
        id: string;
        ticket: {
            id: string;
        };
    };
}
