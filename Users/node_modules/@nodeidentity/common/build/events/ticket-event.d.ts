import { Subjects } from "./subscribs";
export interface TicketCreateEvent {
    subject: Subjects.TicketCreated;
    data: {
        id: string;
        title: string;
        price: number;
        userId: string;
    };
}
