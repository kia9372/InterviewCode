import { Message } from "node-nats-streaming";
import { Listener } from "../../base-listener";
import { Subjects } from "../../subscribs";
import { TicketCreateEvent } from "../../ticket-event";
export declare class CreateTicketListerner extends Listener<TicketCreateEvent> {
    subject: Subjects.TicketCreated;
    queueGroupName: string;
    onMessage(data: {
        id: string;
        title: string;
        price: number;
        userId: string;
    }, msg: Message): void;
}
