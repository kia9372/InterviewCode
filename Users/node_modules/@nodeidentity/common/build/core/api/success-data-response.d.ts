import { ApiResponse, Data } from './ApiResponse';
import { Response } from 'express';
export declare class SuccessDataResponse extends ApiResponse {
    constructor(result: Data, message?: string);
    send(res: Response): Response;
}
